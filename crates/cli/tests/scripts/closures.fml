let j = 0;

let create = closure() {
    let j = 0;

    return closure() {
        j += 1;
      	return j;
    };
};

let a = create();
let b = create();
let c = create();

assert(a() == 1);
assert(b() == 1);
assert(c() == 1);

assert(a() == 2);
assert(b() == 2);
assert(c() == 2);

return true;
