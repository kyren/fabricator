let success, res = pcall(closure(a, b, c) {
        return a + b + c;
    }, 1, 2, 3);
assert(success == true, res == 6);

let success, res = pcall(closure(a, b) {
        return a + b;
    }, "1", {});
assert(success == false);

return true;
